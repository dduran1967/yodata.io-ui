import {default as _Link} from 'react-router-dom/Link';
import styled from 'styled-components';

const Link = styled(_Link)`
  color: inherit;
  &:hover {
    color: inherit;
  }
`

export default Link;